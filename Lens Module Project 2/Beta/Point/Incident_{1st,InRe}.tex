If(

	IsDefined(
		Intersect(
			Ray(Light1,Light2),
			Arc_{1st,InRe},
			1
		)
	) 
		
	∧
		
	!IsDefined(
		Intersect(
			Ray(Light1,Light2),
			Arc_{1st,InRe},
			2
		)
	),
	
	Intersect(
		Ray(Light1,Light2),
		Arc_{1st,InRe},
		1
	),
	
	IsDefined(
		Intersect(
			Ray(Light1,Light2),
			Arc_{1st,InRe},
			2
		)
	)
	
	∧
	
	!IsDefined(
		Intersect(
			Ray(Light1,Light2),
			Arc_{1st,InRe},
			1
		)
	),
	
	Intersect(
		Ray(Light1,Light2),
		Arc_{1st,InRe},
		2
	),

	IsDefined(Intersect(Ray(Light1,Light2),Arc_{1st,InRe},1)) ∧ IsDefined(Intersect(Ray(Light1,Light2),Arc_{1st,InRe},2)),

	If(

		Segment(Intersect(Ray(Light1,Light2),Arc_{1st,InRe},1),Light1) < Segment(Intersect(Ray(Light1,Light2),Arc_{1st,InRe},2),Light1),
		Intersect(Ray(Light1,Light2),Arc_{1st,InRe},1),

		Segment(Intersect(Ray(Light1,Light2),Arc_{1st,InRe},2),Light1) < Segment(Intersect(Ray(Light1,Light2),Arc_{1st,InRe},1),Light1),

		Intersect(Ray(Light1,Light2),Arc_{1st,InRe},2)

	)

)