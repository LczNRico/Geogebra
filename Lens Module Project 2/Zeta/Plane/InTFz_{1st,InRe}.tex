InTFz_{1st,InRe}: 

    If(

        Angle(
        
            Light1,

            Intersect(

                Segment(A,B),

                Plane(IntersectConic(a,b))

            ),

            A
        )

        <((π)/(2)),
        
        Plane(B,Ray(Light1,Light2)),
        
        Angle(
        
            Light1,

            Intersect(

                Segment(A,B),

                Plane(IntersectConic(a,b))

            ),

            A
        )

        >((π)/(2)),
        
        Plane(A,Ray(Light1,Light2)})
        
    )